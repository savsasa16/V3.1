# ระบุเวอร์ชัน Python ที่ Render ควรใช้
# Python 3.11 เป็นเวอร์ชันที่แนะนำสำหรับ Render เนื่องจากมีความเข้ากันได้ดีกับ Library หลายตัว
python_version == "3.11.*"

# WSGI Server
gunicorn==22.0.0

# Web Framework (เลือกใช้แค่ตัวเดียวที่คุณใช้)
Flask==3.0.3       # ถ้าคุณใช้ Flask
# Django==5.0.6    # ถ้าคุณใช้ Django (คอมเมนต์ Flask ออก)

# Database Driver (เลือกตามที่คุณใช้)
# psycopg2-binary==2.9.9 # ถ้าใช้ PostgreSQL
# PyMySQL==1.1.0       # ถ้าใช้ MySQL/MariaDB
# SQLAlchemy==2.0.30     # ถ้าใช้ SQLAlchemy (ไม่ว่าจะเป็นกับ Flask-SQLAlchemy หรือ standalone)
# Flask-SQLAlchemy==3.1.1 # ถ้าคุณใช้ Flask-SQLAlchemy (ต้องมี SQLAlchemy ด้านบนด้วย)

# Core Scientific Libraries (เวอร์ชันที่เข้ากันได้ดีกับ Python 3.11 และ Torch ล่าสุด)
numpy==1.26.4
scipy==1.13.0
numba==0.60.0 # numba 0.60.0 รองรับ numpy 1.22+ แต่ไม่เกิน 2.1
# เนื่องจาก numpy==1.26.4 เป็นเวอร์ชัน 1.26.x ซึ่งเกิน 2.1
# อาจต้องบังคับ numba ให้ใช้เวอร์ชันที่ใหม่กว่า 0.60.0 ที่รองรับ numpy 1.26.4
# หรือยอมรับว่า numba อาจจะไม่ได้ถูกใช้ในส่วนที่ต้องการ numpy 2.x
# หรือใช้ numba เวอร์ชันที่ใหม่กว่าที่รองรับ numpy 2.x+
# เช่น numba==0.59.1 (ถ้ามี) หรือ numba==0.58.1

# ดังนั้น: ถ้าโค้ดของคุณพึ่งพา Numba และ Numpy 2.x มาก ให้ลองใช้ numba=0.59.1 หรือ 0.58.1
# มิฉะนั้น ให้คง numba==0.60.0 ไว้ แต่อาจมีคำเตือน
# หรืออัปเดต numba เป็นเวอร์ชันที่ใหม่กว่า 0.60.0 (ถ้ามีและเข้ากันได้)

# PyTorch (เวอร์ชัน CPU-only ที่คาดว่าจะหา distribution ได้บน Render)
# ลบ +cpu ออกเพื่อให้ pip หา wheel ที่เข้ากันได้กับสภาพแวดล้อมโดยอัตโนมัติ
torch==2.3.1 # แนะนำเวอร์ชันนี้ เพราะค่อนข้างใหม่และมี wheel สำหรับ CPU ที่กว้างขวางกว่า
torchvision==0.18.1 # ถ้าใช้
torchaudio==2.3.1   # ถ้าใช้

# Library อื่นๆ ที่คุณอาจใช้ (เพิ่มเข้ามาเอง)
# pandas==2.2.2
# matplotlib==3.9.0
# scikit-learn==1.4.2
# opencv-python==4.9.0.80
# Pillow==10.3.0
# pymatting==1.1.13 # หากโปรเจกต์คุณใช้ pymatting จริงๆ และยอมรับ conflict กับ numba 0.60.0
                   # หรือถ้า numba ถูกอัปเกรดเป็นเวอร์ชันที่รองรับ numpy 2.x
                   # ควรตรวจสอบ pymatting ว่ารองรับ torch/numpy เวอร์ชันเหล่านี้หรือไม่

# ส่วนเพิ่มเติมที่คุณต้องมีใน requirements.txt
# (ถ้าใช้ตามที่เคยเจอในโค้ดของคุณ)
# Flask-WTF==1.2.1
# WTForms==3.1.2
# email_validator==2.1.1
# python-dotenv==1.0.1
# Werkzeug==3.0.3
# itsdangerous==2.2.0
# click==8.1.7
# Jinja2==3.1.4
# MarkupSafe==2.1.5

# ตัวอย่าง Library ที่คุณอาจใช้ (ถ้ามี)
# หากใช้ Flask-Login
# Flask-Login==0.6.3
# หากใช้ Flask-Migrate
# Flask-Migrate==4.0.5
# หากใช้ Flask-Bcrypt
# Flask-Bcrypt==1.0.1
# Flask-Mail
# Flask-Mail==0.9.1